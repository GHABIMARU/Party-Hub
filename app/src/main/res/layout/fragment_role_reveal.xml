<?xml version="1.0" encoding="utf-8"?>
<!-- Root is a full-screen touchable FrameLayout.
     The touch listener is set on this to capture global press/release. -->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/touch_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_dark"
    android:clickable="true"
    android:focusable="true">

    <!-- ═══════════════════════════════════════════════════════════════
         STATE 1 — INTRO  (visible by default, hidden during press)
         Shows player name and hold instruction.
    ════════════════════════════════════════════════════════════════ -->
    <LinearLayout
        android:id="@+id/layout_intro"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="40dp">

        <!-- Role label -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="YOUR ROLE"
            android:textSize="11sp"
            android:letterSpacing="0.3"
            android:textColor="@color/text_secondary"
            android:layout_marginBottom="32dp"/>

        <!-- Player name — large and prominent -->
        <TextView
            android:id="@+id/tv_player_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="AGENT"
            android:textSize="38sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary"
            android:gravity="center"
            android:layout_marginBottom="48dp"/>

        <!-- Finger icon + hold instruction -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="☞"
            android:textSize="36sp"
            android:gravity="center"
            android:alpha="0.4"
            android:layout_marginBottom="12dp"/>

        <TextView
            android:id="@+id/tv_hint"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Press and hold anywhere\nto see your role"
            android:textSize="15sp"
            android:textColor="@color/text_secondary"
            android:gravity="center"
            android:lineSpacingMultiplier="1.5"/>

    </LinearLayout>

    <!-- ═══════════════════════════════════════════════════════════════
         STATE 2 — REVEAL  (hidden by default, shown on ACTION_DOWN)
         Shows the secret word / impostor message.
    ════════════════════════════════════════════════════════════════ -->
    <LinearLayout
        android:id="@+id/layout_reveal"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="40dp"
        android:visibility="invisible">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="YOUR ROLE"
            android:textSize="11sp"
            android:letterSpacing="0.3"
            android:textColor="@color/text_secondary"
            android:layout_marginBottom="32dp"/>

        <!-- The word / role — the hero of the reveal state -->
        <TextView
            android:id="@+id/role_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="42sp"
            android:textStyle="bold"
            android:textColor="@color/gold"
            android:gravity="center"
            android:lineSpacingMultiplier="1.2"
            android:layout_marginBottom="24dp"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Release to hide"
            android:textSize="12sp"
            android:letterSpacing="0.15"
            android:textColor="@color/text_hint"
            android:gravity="center"/>

    </LinearLayout>

    <!-- ═══════════════════════════════════════════════════════════════
         CONTINUE BUTTON — anchored to bottom, hidden until first reveal
    ════════════════════════════════════════════════════════════════ -->
    <Button
        android:id="@+id/next_btn"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="40dp"
        android:background="@drawable/bg_gold_button"
        android:text="CONTINUE →"
        android:textColor="@color/bg_dark"
        android:textSize="13sp"
        android:textStyle="bold"
        android:letterSpacing="0.12"
        android:stateListAnimator="@null"
        android:visibility="gone"/>

    <!-- Network waiting label — anchored to bottom -->
    <TextView
        android:id="@+id/tv_waiting"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="48dp"
        android:text="⏳  Waiting for other agents..."
        android:textSize="13sp"
        android:textColor="@color/text_secondary"
        android:visibility="gone"/>

</FrameLayout>